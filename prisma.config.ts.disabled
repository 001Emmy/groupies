// This configuration is optional - Prisma works without it for basic usage
// To use this, install: npm install --save-dev prisma dotenv
import "dotenv/config";

export default {
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
};
